services:
  backend:
    build:
      context: ./backend
    expose:
      - "3000"

  frontend:
    build:
      context: .
      args:
        VITE_API_BASE_URL: /api
    ports:
      - "8081:80"
    depends_on:
      - backend